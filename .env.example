DATABASE_URL="postgresql://heair:heair@localhost:5432/heair?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
# Set these only in Render or another server-side secret manager. Do not commit
# real administrator credentials or expose them in browser JavaScript.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
PORT=3000
# Server-side AI only. Never put this key in GitHub Pages, browser JavaScript, or commits.
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-sonnet-4-6"
# Optional: used by the static GitHub Pages survey to request an AI-enhanced report.
# PUBLIC_API_URL="https://your-api.example.com"
