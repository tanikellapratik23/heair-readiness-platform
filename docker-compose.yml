services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: heair
      POSTGRES_PASSWORD: heair
      POSTGRES_DB: heair
    ports:
      - "5432:5432"
    volumes:
      - heair-postgres:/var/lib/postgresql/data
volumes:
  heair-postgres:
